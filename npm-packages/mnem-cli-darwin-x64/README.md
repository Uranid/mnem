# mnem-cli-darwin-x64

Prebuilt `mnem` binary for macOS Intel hosts. Ships the `aarch64-apple-darwin` binary; runs under Rosetta 2 on Intel Macs. Do not install directly; depend on `mnem-cli` instead.
